#include <stdio.h>

/****************
 * this exercise just prints out an answer
 * to the question posed to the reader in
 * "The 'C' Programming Language"
 ****************/

int main() {

    printf(" I would test the word count program by passing in input
            consisting of different numbers of words to verify that
            the basic functionality is there. I would then begin to
            include characters other than the normal alphanumeric
            set to see if the system correctly handled those edge cases.
            The word count program has a bug where it will count any
            character as a word if it is surrounnded by any of the known
            non-word characters");

    return 0;
}
